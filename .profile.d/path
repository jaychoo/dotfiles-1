# Prepend to path
if [ `brew list | grep coreutils` ]; then
    export PATH=`brew --prefix coreutils`:$PATH
fi
export PATH=~/bin:/usr/local/bin:/usr/local/sbin:/usr/local/share/python:$PATH

# Append to path
export PATH=$PATH:~/.ghar/bin:/opt/pdflabs/pdftk/bin:/usr/local/share/npm/bin

